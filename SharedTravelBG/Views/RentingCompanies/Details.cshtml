@model SharedTravelBG.Models.RentingCompany

@{
    ViewData["Title"] = "Подробности за компания";
}

<h1>Подробности за компания</h1>

<div>
    <h4>Наемна компания</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Име</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Адрес</dt>
        <dd class="col-sm-10">@Model.Address</dd>
        <dt class="col-sm-2">Телефонен номер</dt>
        <dd class="col-sm-10">@Model.PhoneNumber</dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Редактирай</a> |
    <a asp-action="Index">Обратно към списъка</a>
</div>
